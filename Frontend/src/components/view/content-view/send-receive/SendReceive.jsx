import React from 'react';
import Headers from '../components/Headers';

const SendReceive = () => {
  return (
    <div className="flex flex-col">
      <Headers
        title="Send & Receive"
        description="Send and receive tokens to and from your wallet."
      />
    </div>
  );
};

export default SendReceive;
