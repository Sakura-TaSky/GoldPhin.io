import React from 'react';
import Headers from '../components/Headers';

const Settings = () => {
  return (
    <div className="flex flex-col">
      <Headers title="Settings" description="Manage your wallet settings." />
    </div>
  );
};

export default Settings;
